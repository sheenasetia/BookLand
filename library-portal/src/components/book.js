/*import React from 'react';

export class Book extends React.Component {
    render() {
        return (
            <div>
            <p>{this.props.book.name}</p>
            <p>{this.props.book.author}</p>
            <p>{this.props.book.version}</p>
            <button onClick={()=>this.props.onremove(this.props.book.id)}>REMOVE BOOK</button>
            </div>
        ) 
    }
}*/